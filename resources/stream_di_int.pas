program ideone;
const N_TIMES = 10;
var a, i : integer;
begin
   for i := 1 to N_TIMES do
   begin
      Read(a);
      Write(a, ' ');
   end;
end.

(* argomento: esercitazione sui cicli a conteggio
 * problema :
   scrivere un programma che legga 10 interi da input
   e poi li scriva su output.
 *)

