apiVersion: v2
name: cmsocial
description: A web application that builds a social coding platform upon CMS

# Update this every time the appVersion is updated
version: 0.0.4
# This should match the tag of the images
appVersion: v4
type: application

dependencies:
  - name: cms
    version: "0.0.4"
    repository: https://edomora97.github.io/cms/helm/
